apiVersion: v1
kind: Service
metadata:
  name: {{ include "eso-sops-server.fullname" . }}
  labels:
    {{- include "eso-sops-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eso-sops-server.selectorLabels" . | nindent 4 }}
